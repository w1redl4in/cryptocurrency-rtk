import { Page, Text } from '@geist-ui/react';
export const PageHeader = () => {
  return (
    <Page.Header center>
      <Text h3 mt={2}>
        Real-time crypto coins statistics
      </Text>
    </Page.Header>
  );
};
